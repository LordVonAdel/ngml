global money;

class persistent Test1 {
  var a = 0;
  var b = 3;

  Test1(a, c, f) {
    b++;
    a = 0;
  }

  method stinken(intensity) {
    a += intensity * b;
  }
/*
  method stinken2 () {

  }

  event step {
    a += 4;
  }

  event stepBegin {

  }

  event draw {
    draw_rectangle(1, 1, 3, 3, false);
  }
*/

//

//
}

function dingsBums(dings, bums) {
  return (dings * bums) / (dings - bums);
}